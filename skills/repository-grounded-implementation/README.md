# repository-grounded-implementation

## Purpose

Produce implementation guidance and patches that align with the existing codebase.

## Responsibilities

- retrieve similar code
- infer patterns from the repository
- generate patch-oriented changes
- recommend tests and verification scope

## Triggering

This skill should be triggered by `bp:find-reference` and `bp:implement`. Reference retrieval should happen before implementation.
